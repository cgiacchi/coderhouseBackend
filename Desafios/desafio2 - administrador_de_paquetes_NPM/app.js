// const fs = require('fs');




// function rangoNumeros(min,max){
//     return Math.floor(math.random()(min,max));

// }

// function generarNumeros(){
//     const numeros={};

// }

// // Función para generar un número aleatorio en un rango específico
// function generarNumero(min, max) {
//     return Math.floor(Math.random() * (max - min + 1) + min);
// }
// // Función principal para generar los números y contar su frecuencia
// function generarNumeros() {
//     const numeros = {};

//     for (let i =0, i<10000; i++){
//         const numero = generarNumero(1,20);
//     }
//     return numeros;

// function imprimirResultados(resultados) {
//     for (const numero in resultados) {
//         console.log(`Número ${numero}: ${resultados[numero]} veces`);
//     }
// }

// // Función para escribir los resultados en un archivo
// function escribirResultadosEnArchivo(resultados) {
//     const data = JSON.stringify(resultados, null, 2);
//     fs.writeFileSync('resultados.json', data);

// const resultados = generarNumeros();

// imprimirResultados(resultados);
// }

// escribirResultadosEnArchivo(resultados);
// }
